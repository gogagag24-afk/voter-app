import React from "react";
import ReactDOM from "react-dom/client";
import VoterApp from "./voter-app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VoterApp />
  </React.StrictMode>
);